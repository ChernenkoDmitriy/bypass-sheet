import React, { FC } from 'react';
import { observer } from 'mobx-react';
import { ScreenContainer } from '../../../../UIKit/screenContainer';

export const AuthorizationPhoneView: FC = observer(() => {

    return (
        <ScreenContainer edges={['top']}>

        </ScreenContainer>
    );
});